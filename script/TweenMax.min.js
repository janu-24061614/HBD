// TweenMax.min.js placeholder
// Tracking/Privacy protections in some browsers can interfere with loading third-party
// scripts from CDNs. To avoid that, download GSAP (TweenMax) and place the minified
// file here at `script/TweenMax.min.js`.
//
// Download link (example): https://cdnjs.cloudflare.com/ajax/libs/gsap/1.20.3/TweenMax.min.js
// After placing the file here, reload the page. If you prefer, you can also use a
// later GSAP version and adjust the code accordingly.

if (typeof TweenMax === 'undefined') {
  console.warn('GSAP TweenMax not found. Please download TweenMax.min.js into script/TweenMax.min.js');
  // Provide a very small shim so code that references TweenMax fails gracefully
  window.TweenMax = {
    to: function() { console.warn('TweenMax.to called — shim no-op'); },
    from: function() { console.warn('TweenMax.from called — shim no-op'); },
    staggerFrom: function() { console.warn('TweenMax.staggerFrom called — shim no-op'); }
  };
}
